env: "local" #local, dev, prod
http_server:
  address: "localhost:8080"
  timeout: 4s #время на чтения запроса и такое же на обработку
  idle_timeout: 60s #время жизни соединения
  user: "myuser"
  password: "mypass"